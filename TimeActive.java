
/**
 * Write a description of interface TimeActive here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public interface TimeActive
{
    void actAtTime(Field currentField, Field nextFieldState, boolean isNight);
}
